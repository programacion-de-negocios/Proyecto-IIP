<Window x:Class="Proyecto_IIP.Vehiculo"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Proyecto_IIP"
        mc:Ignorable="d"
        Title="Vehiculo" Height="450" Width="800">
    <Grid>
        <StackPanel Orientation="Horizontal">
        </StackPanel>
        <ListBox  HorizontalContentAlignment="Stretch" Name="lbvehiculo" Margin="0,30,0,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding Placa}"/>
                        <TextBlock Grid.Column="1" Text="{Binding Tipo}"/>
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
